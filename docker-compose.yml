version: "3.8"

services:
  city-bcp-agent:
    build: .
    container_name: city-bcp-agent
    ports:
      - "8501:8501"
    volumes:
      - bcp-data:/app/db
    env_file:
      - .env
    restart: unless-stopped

volumes:
  bcp-data:
